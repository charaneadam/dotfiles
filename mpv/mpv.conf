# Prevents the "darkening" / gamma shift in fullscreen
target-trc=srgb

# Prevents mpv from sending color hints to the Wayland compositor
target-colorspace-hint=no

# Recommended: ensures consistent behavior across different video outputs
vo=gpu-next
